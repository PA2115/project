import React from 'react';
//import Container  from 'react-bootstrap/Container';

 const Layout = (props) =>(
    // <Container></Container>
    <React.Fragment>
        {props.children}
    </React.Fragment>
)
export default Layout;
